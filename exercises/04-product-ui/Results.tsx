// TODO 04: render structured tool results as grounded product cards; use the supplied GroundedResults component.
export function Results(_props: { name: string; output: unknown }) {
  return null;
}
